[package]
name = "mlp_merge"
version = "0.1.0"
authors = ["Felix Buehler <account@buehler.rocks>"]
edition = "2018"

[dependencies]
clap = { version = "3.1", features = ["cargo"] }
rand = "0.8"
rayon = "1.5"
ordered-float = "3.0"
